# PR 42: add import apply dry run

This change adds a dry-run preview for accepted source and claim records before any durable import apply.

The preview reports `would_write` ids and blocks non-approved apply requests.
