# Research Example

Use AKBP to compile sources into durable research knowledge.

Example flow:

```text
raw papers/articles/videos
→ claims with evidence
→ concept pages
→ entity graph
→ synthesis page
→ future queries retrieve compiled understanding
```
